Public Class clsBitacoraRegistro

  
    Public Function Registro(ByVal tbl As String, ByVal accion As String, ByVal valori As String, ByVal valorf As String, ByVal observacion As String)

        'Dim bitacora As New clsBitacora

        'Dim fecha As String = Format(Now, "dd-MM-yyyy")
        'Dim coduser As String = mdlPublicVars.idUsuario.ToString
        'Dim hora As String = Format(Now, "hh:mm:ss")

        'bitacora.coduser = coduser.ToString
        'bitacora.fecha = fecha.ToString + " " + hora.ToString
        'bitacora.tbl = tbl.ToString
        'bitacora.accion = accion.ToString
        'bitacora.valorInicial = valori.ToString
        'bitacora.valorFinal = valorf.ToString
        'bitacora.observacion = observacion.ToString
        'bitacora.Crear()

        Return 0
    End Function

End Class
